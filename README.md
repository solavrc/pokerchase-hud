# pr-assets

PRレビュー用のスクリーンショット置き場。**マージ対象ではありません**。
`main` の履歴とは独立した orphan ブランチなので、不要になったら
`git push origin --delete pr-assets` で消せます。

画像は `e2e/tools/capture-popup-themes.ts` と同じ仕組み（実ブラウザで
拡張のポップアップを開いて撮影）で生成しています。
